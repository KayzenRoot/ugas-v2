# GENERATED-DEEP-PREPROGRAMMED
# Focused A0/A1 contract surface for M18. No GPU/network/provider calls here.

def test_contract_surface_is_declared():
    expected = ['LocaleProfile', 'LocalizedText', 'CulturalConstraint', 'DubPlan', 'SubtitlePlan', 'LocalizationEvaluation']
    assert expected

def test_service_surface_is_declared():
    expected = ['LocalizationPlanner', 'TranslationService', 'CulturalValidationService', 'DubSubtitleService']
    assert expected

# CODEX-TASK[M18-TESTS]: replace declaration checks with module-specific invariants and fake-port orchestration tests from the wave manifest.

# GENERATED-DEEP-PREPROGRAMMED
from typing import Any, Protocol

class HumanAssetStore(Protocol):
    """M06 boundary. Provider/framework details stay behind this port."""
    async def execute(self, request: Any) -> Any: ...

class ConsentRegistry(Protocol):
    """M06 boundary. Provider/framework details stay behind this port."""
    async def execute(self, request: Any) -> Any: ...

class FaceEvaluator(Protocol):
    """M06 boundary. Provider/framework details stay behind this port."""
    async def execute(self, request: Any) -> Any: ...

class BodyEvaluator(Protocol):
    """M06 boundary. Provider/framework details stay behind this port."""
    async def execute(self, request: Any) -> Any: ...


# GENERATED-DEEP-PREPROGRAMMED
# Focused A0/A1 contract surface for M31. No GPU/network/provider calls here.

def test_contract_surface_is_declared():
    expected = ['CreativeIntentKernel', 'CreativeConstraint', 'CreativeDecision', 'VisualEmotionMap', 'CreativeWorldModel', 'DirectionBrief']
    assert expected

def test_service_surface_is_declared():
    expected = ['CreativeDirector', 'ConstraintLedgerService', 'DirectionCompiler', 'CreativeEvaluationService']
    assert expected

# CODEX-TASK[M31-TESTS]: replace declaration checks with module-specific invariants and fake-port orchestration tests from the wave manifest.
